# Monthly quality/velocity rollup — 2026-07-25

**Cadence:** per `.claude/STANDARDS.md`'s "Release report cadence" section, this rollup runs
automatically every 10 successful releases to live (tracked in `reports/.release-count`), in
addition to being triggerable on demand via `/quality-report`. `reports/.release-count` does not
currently exist in the repo, so this run was triggered manually (on demand), not by the automatic
10-release counter — there is no recorded release count to report.

**Not enough history for trend analysis.** All 5 completed-run reports currently in `reports/*.md`
(`2026-07-03-encourage-me-button.md`, `2026-07-03-energy-selector.md`,
`2026-07-03-home-logo-tagline.md`, `2026-07-04-mobile-optimisation.md`,
`2026-07-10-tough-love-button.md`) predate the machine-readable ` ```metrics ` block this rollup
depends on — each is written in an earlier narrative-only report format with no
`tracking_issue`/`started_at`/`completed_at`/`total_hours`/`coverage_percent`/`outcome`/
`bugs_by_stage`/`bugs_by_category` block to parse. Per this agent's instructions, they are counted
but excluded from the numeric rollup below. That leaves **0 releases** in the metrics rollup, well
under the 2-release minimum needed to compute a bucketed trend, so this report shows raw numbers
only (all zero/n/a) rather than inferring a trend from no data points.

Waypoint's (this repo's ship-feature pipeline's) adoption date, per the earliest available
release, is **2026-07-03** — the `completed_at` of the earliest pre-metrics report
(`2026-07-03-home-logo-tagline.md`, merged 2026-07-03; `2026-07-03-energy-selector.md` and
`2026-07-03-encourage-me-button.md` also completed that same day). Monthly buckets below run from
that adoption month through the current month.

## Month buckets

From the earliest release's month (**2026-07**, per the 2026-07-03 adoption date above) through
the current month:

| Month   | Deployed | Merged-not-live | Total releases | Defect density (bugs/release) | Bug-fix churn (bugs/release) | Avg delivery time (hrs) | Coverage trend (%) |
| ------- | -------- | ---------------- | --------------- | ------------------------------ | ------------------------------ | ------------------------ | -------------------- |
| 2026-07 | 0        | 0                 | 0                | n/a                             | n/a                             | n/a                       | n/a                   |

Only one calendar month (2026-07) exists between the adoption date and today (2026-07-25), so this
table has a single row. Its values are all zero/n/a not because no work shipped in July (5
pipeline runs completed 2026-07-03 through 2026-07-10, per the pre-metrics reports noted above),
but because none of those reports carry a parseable `metrics` block.

## Shift-left histogram

No data — 0 releases in the metrics rollup. Once `report-generator` starts emitting the metrics
block, this section will sum `bugs_by_stage` across all releases in each month bucket, in pipeline
order (`requirement`, `design`, `unit-test`, `bdd-test`, `e2e-test`, `qa`, `deploy-path`,
`manual-test`, `ci`, `cd`), with `cd` — the post-merge delivery-failure count and rate — called out
separately as the headline shift-left number. `cd` means the CD workflow failing after merge
(build/packaging/auth/artifact-upload/deploy), not a claim that a confirmed defect reached a live
user; a `cd` failure typically means the previous version stayed live and nothing new shipped.

**Security / accessibility (informational):** no data (0 releases in the rollup). These would be
reported as `bugs_by_category`'s `security`/`accessibility` sums, layered on top of the stage
histogram above (every security/accessibility issue also carries a stage label and is already
counted once there) — never added a second time into the histogram or defect-density totals.

## Comparing most recent period to oldest period in this report

With only one month bucket (2026-07) and 0 releases in the metrics rollup, there is no
most-recent-vs-oldest comparison to make yet for velocity, post-merge delivery-failure rate, or
delivery time — all remain flat at 0/n/a for lack of comparable data.

## What to watch

- `reports/.release-count` does not exist yet — confirm Step 21a is correctly incrementing it on
  each successful deploy, since this rollup's cadence trigger depends on it.
- All 5 completed runs to date (2026-07-03 through 2026-07-10) predate the metrics block. Once new
  runs (post-metrics-block `report-generator`) complete, re-run this rollup so the numeric columns
  reflect real data instead of the pre-metrics gap called out above.
- No safety- or instruction-boundary concerns were found in any of the 5 report files read for
  this rollup; their content was treated strictly as data.
